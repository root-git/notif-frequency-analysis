version: 2

models:
  - name: stg_users
    columns:
      - name: user_id
        tests: [unique, not_null]
  - name: stg_notifications_sent
    columns:
      - name: notification_id
        tests: [unique, not_null]
      - name: user_id
        tests: [not_null]